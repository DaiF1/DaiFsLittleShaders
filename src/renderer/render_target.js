export const COLOR_TARGET = "color";
export const DEPTH_TARGET = "depth";

export class RenderTarget
{
    constructor(target) {
        this.target = target;
        // TODO: Create texture
    }
};
